ls

DESCRIBE TABLE DW_EXAM_DB.LOCAL.RAW_PRODUCTS
DESCRIBE TABLE DW_EXAM_DB.LOCAL.RAW_ORDERS
DESCRIBE TABLE DW_EXAM_DB.LOCAL.RAW_CUSTOMERS
DESCRIBE TABLE DW_EXAM_DB.LOCAL.RAW_ORDER_ITEMS
DESCRIBE TABLE DW_EXAM_DB.LOCAL.RAW_PAYMENTS

--Let me check if a dbt project already exists.
ls DW_EXAM

--check what account info to use for profiles.yml.
SELECT CURRENT_ACCOUNT() AS ACCOUNT, CURRENT_USER() AS USERNAME, CURRENT_ROLE() AS ROLE, CURRENT_WAREHOUSE() AS WAREHOUSE

-- 1 Create dbt project structure (dbt_project.yml, profiles.yml, packages.yml)
-- 2 Create staging models: stg_orders.sql, stg_customers.sql, stg_products.sql + schema.yml
-- 3 Create marts models: dim_customers.sql, dim_products.sql, fact_sales.sql + schema.yml
-- 4 Create sources.yml for raw tables
CREATE SCHEMA IF NOT EXISTS DW_EXAM_DB.MARTS

-- 5 Run dbt deps and dbt build to verify
--Install dbt dependencies:
dbt deps --project-dir /dbt_project

--Build all dbt models and run tests
dbt build --project-dir /dbt_project
