create or replace database DW_EXAM_DB;

create or replace schema DW_EXAM_DB.LOCAL;

create or replace TABLE DW_EXAM_DB.LOCAL.DIM_CUSTOMERS (
	CUSTOMER_ID VARCHAR(16777216),
	CUSTOMER_UNIQUE_ID VARCHAR(16777216),
	CUSTOMER_ZIP_CODE_PREFIX NUMBER(38,0),
	CUSTOMER_CITY VARCHAR(16777216),
	CUSTOMER_STATE VARCHAR(16777216)
);
create or replace TABLE DW_EXAM_DB.LOCAL.DIM_DATES (
	YEAR NUMBER(4,0),
	MONTH NUMBER(2,0),
	DAY NUMBER(2,0),
	FULL_DATE DATE
);
create or replace TABLE DW_EXAM_DB.LOCAL.DIM_PRODUCTS (
	PRODUCT_ID VARCHAR(16777216),
	PRODUCT_CATEGORY_NAME VARCHAR(16777216),
	PRODUCT_WEIGHT_G NUMBER(38,0),
	PRODUCT_LENGTH_CM NUMBER(38,0),
	PRODUCT_HEIGHT_CM NUMBER(38,0),
	PRODUCT_WIDTH_CM NUMBER(38,0)
);
create or replace TABLE DW_EXAM_DB.LOCAL.FACT_SALES (
	ORDER_ID VARCHAR(16777216),
	CUSTOMER_ID VARCHAR(16777216),
	PRODUCT_ID VARCHAR(16777216),
	ORDER_DATE TIMESTAMP_NTZ(9),
	PRICE NUMBER(38,2),
	FREIGHT_VALUE NUMBER(38,2),
	SALES_AMOUNT NUMBER(38,2),
	PAYMENT_TYPE VARCHAR(16777216),
	SEQ NUMBER(18,0)
);
create or replace TABLE DW_EXAM_DB.LOCAL.RAW_CUSTOMERS (
	CUSTOMER_ID VARCHAR(16777216),
	CUSTOMER_UNIQUE_ID VARCHAR(16777216),
	CUSTOMER_ZIP_CODE_PREFIX NUMBER(38,0),
	CUSTOMER_CITY VARCHAR(16777216),
	CUSTOMER_STATE VARCHAR(16777216)
);
create or replace TABLE DW_EXAM_DB.LOCAL.RAW_ORDERS (
	ORDER_ID VARCHAR(16777216),
	CUSTOMER_ID VARCHAR(16777216),
	ORDER_STATUS VARCHAR(16777216),
	ORDER_PURCHASE_TIMESTAMP TIMESTAMP_NTZ(9),
	ORDER_APPROVED_AT TIMESTAMP_NTZ(9),
	ORDER_DELIVERED_CARRIER_DATE TIMESTAMP_NTZ(9),
	ORDER_DELIVERED_CUSTOMER_DATE TIMESTAMP_NTZ(9),
	ORDER_ESTIMATED_DELIVERY_DATE TIMESTAMP_NTZ(9)
);
create or replace TABLE DW_EXAM_DB.LOCAL.RAW_ORDER_ITEMS (
	ORDER_ID VARCHAR(16777216),
	ORDER_ITEM_ID NUMBER(38,0),
	PRODUCT_ID VARCHAR(16777216),
	SELLER_ID VARCHAR(16777216),
	SHIPPING_LIMIT_DATE TIMESTAMP_NTZ(9),
	PRICE NUMBER(38,2),
	FREIGHT_VALUE NUMBER(38,2)
);
create or replace TABLE DW_EXAM_DB.LOCAL.RAW_PAYMENTS (
	ORDER_ID VARCHAR(16777216),
	PAYMENT_SEQUENTIAL NUMBER(38,0),
	PAYMENT_TYPE VARCHAR(16777216),
	PAYMENT_INSTALLMENTS NUMBER(38,0),
	PAYMENT_VALUE NUMBER(38,2)
);
create or replace TABLE DW_EXAM_DB.LOCAL.RAW_PRODUCTS (
	PRODUCT_ID VARCHAR(16777216),
	PRODUCT_CATEGORY_NAME VARCHAR(16777216),
	PRODUCT_NAME_LENGHT NUMBER(38,0),
	PRODUCT_DESCRIPTION_LENGHT NUMBER(38,0),
	PRODUCT_PHOTOS_QTY NUMBER(38,0),
	PRODUCT_WEIGHT_G NUMBER(38,0),
	PRODUCT_LENGTH_CM NUMBER(38,0),
	PRODUCT_HEIGHT_CM NUMBER(38,0),
	PRODUCT_WIDTH_CM NUMBER(38,0)
);


-- 1. fact table: fact_sales
CREATE OR REPLACE TABLE DW_EXAM_DB.LOCAL.FACT_SALES AS
WITH A AS (
SELECT
    o.ORDER_ID AS ORDER_ID,
    o.CUSTOMER_ID,
    oi.PRODUCT_ID,
    o.ORDER_PURCHASE_TIMESTAMP AS ORDER_DATE,
    oi.PRICE,
    oi.FREIGHT_VALUE,
    p.PAYMENT_VALUE AS SALES_AMOUNT,
    p.PAYMENT_TYPE,
    ROW_NUMBER()OVER(PARTITION BY o.ORDER_ID ORDER BY o.ORDER_ID) AS SEQ
FROM DW_EXAM_DB.LOCAL.RAW_ORDERS o
LEFT JOIN DW_EXAM_DB.LOCAL.RAW_ORDER_ITEMS oi
    ON o.ORDER_ID = oi.ORDER_ID
LEFT JOIN DW_EXAM_DB.LOCAL.RAW_PAYMENTS p
    ON o.ORDER_ID = p.ORDER_ID
)
SELECT A.* FROM A WHERE A.SEQ = 1;


-- 2. Dimension tables: 
   -- 2a. dim_customers
CREATE OR REPLACE TABLE DW_EXAM_DB.LOCAL.DIM_CUSTOMERS AS
SELECT DISTINCT
    CUSTOMER_ID,
    CUSTOMER_UNIQUE_ID,
    CUSTOMER_ZIP_CODE_PREFIX,
    CUSTOMER_CITY,
    CUSTOMER_STATE
FROM DW_EXAM_DB.LOCAL.RAW_CUSTOMERS;
   
   -- 2b. dim_products
CREATE OR REPLACE TABLE DW_EXAM_DB.LOCAL.DIM_PRODUCTS AS
SELECT DISTINCT
    PRODUCT_ID,
    PRODUCT_CATEGORY_NAME,
    PRODUCT_WEIGHT_G,
    PRODUCT_LENGTH_CM,
    PRODUCT_HEIGHT_CM,
    PRODUCT_WIDTH_CM
FROM DW_EXAM_DB.LOCAL.RAW_PRODUCTS;

   -- 2c. dim_date
CREATE OR REPLACE TABLE DW_EXAM_DB.LOCAL.DIM_DATES AS
SELECT DISTINCT
    YEAR(ORDER_PURCHASE_TIMESTAMP) AS YEAR,
    MONTH(ORDER_PURCHASE_TIMESTAMP) AS MONTH,
    DAY(ORDER_PURCHASE_TIMESTAMP) AS DAY,
    ORDER_PURCHASE_TIMESTAMP::DATE AS FULL_DATE
FROM DW_EXAM_DB.LOCAL.RAW_ORDERS;



create or replace schema DW_EXAM_DB.LOCAL_LOCAL;

create or replace view DW_EXAM_DB.LOCAL_LOCAL.STG_CUSTOMERS(
	CUSTOMER_ID,
	CUSTOMER_UNIQUE_ID,
	CUSTOMER_ZIP_CODE_PREFIX,
	CUSTOMER_CITY,
	CUSTOMER_STATE
) as (
    SELECT
    CUSTOMER_ID,
    CUSTOMER_UNIQUE_ID,
    CUSTOMER_ZIP_CODE_PREFIX,
    CUSTOMER_CITY,
    CUSTOMER_STATE
FROM DW_EXAM_DB.LOCAL.RAW_CUSTOMERS
  );
create or replace view DW_EXAM_DB.LOCAL_LOCAL.STG_ORDERS(
	ORDER_ID,
	CUSTOMER_ID,
	ORDER_STATUS,
	ORDER_PURCHASE_TIMESTAMP,
	ORDER_APPROVED_AT,
	ORDER_DELIVERED_CARRIER_DATE,
	ORDER_DELIVERED_CUSTOMER_DATE,
	ORDER_ESTIMATED_DELIVERY_DATE
) as (
    SELECT
    ORDER_ID,
    CUSTOMER_ID,
    ORDER_STATUS,
    ORDER_PURCHASE_TIMESTAMP,
    ORDER_APPROVED_AT,
    ORDER_DELIVERED_CARRIER_DATE,
    ORDER_DELIVERED_CUSTOMER_DATE,
    ORDER_ESTIMATED_DELIVERY_DATE
FROM DW_EXAM_DB.LOCAL.RAW_ORDERS
  );
create or replace view DW_EXAM_DB.LOCAL_LOCAL.STG_PRODUCTS(
	PRODUCT_ID,
	PRODUCT_CATEGORY_NAME,
	PRODUCT_NAME_LENGHT,
	PRODUCT_DESCRIPTION_LENGHT,
	PRODUCT_PHOTOS_QTY,
	PRODUCT_WEIGHT_G,
	PRODUCT_LENGTH_CM,
	PRODUCT_HEIGHT_CM,
	PRODUCT_WIDTH_CM
) as (
    SELECT
    PRODUCT_ID,
    PRODUCT_CATEGORY_NAME,
    PRODUCT_NAME_LENGHT,
    PRODUCT_DESCRIPTION_LENGHT,
    PRODUCT_PHOTOS_QTY,
    PRODUCT_WEIGHT_G,
    PRODUCT_LENGTH_CM,
    PRODUCT_HEIGHT_CM,
    PRODUCT_WIDTH_CM
FROM DW_EXAM_DB.LOCAL.RAW_PRODUCTS
  );
  
  
  
create or replace schema DW_EXAM_DB.LOCAL_MARTS;

create or replace TRANSIENT TABLE DW_EXAM_DB.LOCAL_MARTS.DIM_CUSTOMERS (
	CUSTOMER_ID VARCHAR(16777216),
	CUSTOMER_UNIQUE_ID VARCHAR(16777216),
	CUSTOMER_ZIP_CODE_PREFIX NUMBER(38,0),
	CUSTOMER_CITY VARCHAR(16777216),
	CUSTOMER_STATE VARCHAR(16777216)
);
create or replace TRANSIENT TABLE DW_EXAM_DB.LOCAL_MARTS.DIM_PRODUCTS (
	PRODUCT_ID VARCHAR(16777216),
	PRODUCT_CATEGORY_NAME VARCHAR(16777216),
	PRODUCT_WEIGHT_G NUMBER(38,0),
	PRODUCT_LENGTH_CM NUMBER(38,0),
	PRODUCT_HEIGHT_CM NUMBER(38,0),
	PRODUCT_WIDTH_CM NUMBER(38,0)
);
create or replace TRANSIENT TABLE DW_EXAM_DB.LOCAL_MARTS.FACT_SALES (
	ORDER_ID VARCHAR(16777216),
	CUSTOMER_ID VARCHAR(16777216),
	PRODUCT_ID VARCHAR(16777216),
	ORDER_DATE TIMESTAMP_NTZ(9),
	PRICE NUMBER(38,2),
	FREIGHT_VALUE NUMBER(38,2),
	SALES_AMOUNT NUMBER(38,2),
	PAYMENT_TYPE VARCHAR(16777216)
);
create or replace schema DW_EXAM_DB.PUBLIC;
